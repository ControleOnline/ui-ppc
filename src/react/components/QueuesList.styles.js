import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  queuesWrapper: {
    width: '100%',
    alignItems: 'center',
    marginTop: 4,
    gap: 2,
  },
});

export default styles;
